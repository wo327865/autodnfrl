[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "autodnf-py"
version = "0.1.0"
description = "Python macOS PlayCover automation for DNF Mobile"
requires-python = ">=3.11"

[tool.setuptools]
packages = ["autodnf_py"]
